# mutual-watchdog
Mutual watchdog between my home lab (go) and an external esp32 (rust), for notifying me if any of them is down

## ESP32

Rust firmware for ESP32 (Xtensa) using ESP-IDF v5.3. Currently connects to WiFi on startup.
