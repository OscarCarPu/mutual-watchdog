services:
  consumer-watchdog:
    build: ./consumer
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "2"
    env_file:
      - .env
      - consumer/.env
